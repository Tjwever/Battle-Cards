# eslint-plugin-react@7.x declares a peer range that caps at eslint 9.7, but this
# project intentionally runs a newer eslint. The plugin works fine against it;
# legacy peer resolution lets a bare `npm install` / `npm ci` succeed without a
# manual --legacy-peer-deps flag (CI already passes this flag explicitly).
legacy-peer-deps=true
