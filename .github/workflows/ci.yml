name: CI

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]

jobs:
  test:
    name: Lint, Type Check & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npm run type-check

      - name: Run tests
        run: npm run test -- --run

  # ─────────────────────────────────────────────────────────────────────
  # AI Code Review (uncomment when API key is available)
  # ─────────────────────────────────────────────────────────────────────
  # ai-review:
  #   name: AI Code Review
  #   runs-on: ubuntu-latest
  #   if: github.event_name == 'pull_request'
  #   permissions:
  #     contents: read
  #     pull-requests: write
  #
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0
  #
  #     # Option A: Kiro CLI (preferred for company use)
  #     # - name: Install Kiro CLI
  #     #   run: curl -fsSL https://cli.kiro.dev/install | bash
  #     # - name: Run Kiro code review
  #     #   env:
  #     #     KIRO_API_KEY: ${{ secrets.KIRO_API_KEY }}
  #     #   run: |
  #     #     kiro-cli chat \
  #     #       --agent code-reviewer \
  #     #       --no-interactive \
  #     #       "Review the changed files in this PR against our project standards in .kiro/steering/review-standards.md"
  #
  #     # Option B: AI Diff Reviewer with Anthropic (placeholder)
  #     # - uses: DailybotHQ/ai-diff-reviewer@v2
  #     #   with:
  #     #     api-key: ${{ secrets.ANTHROPIC_API_KEY }}
  #     #     github-token: ${{ secrets.GITHUB_TOKEN }}
  #     #     provider: anthropic
  #     #     model: claude-sonnet-4-6
  #     #     strictness: block-on-critical
  #     #     prompt-extension-file: .review/extension.md
